use strict;
use warnings;
use utf8;
binmode STDIN, ':encoding(cp932)';
binmode STDOUT, ':encoding(cp932)';
binmode STDERR, ':encoding(cp932)';

my $num;

$num = exp(1);
print "exp(1) = $num\n";

$num = exp(2);
print "exp(2) = $num\n";
