use strict;
use warnings;
use utf8;
binmode STDIN, ':encoding(cp932)';
binmode STDOUT, ':encoding(cp932)';
binmode STDERR, ':encoding(cp932)';

my $num;

$num = sqrt(16);
print "sqrt(16) = $num\n";

$num = sqrt(2);
print "sqrt(2) = $num\n";
